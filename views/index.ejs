<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Oauth Frontend</title>
</head>

<body>
    <% if(access_token){ %>
        <p>Access Token: <%= access_token %>
        </p>
        <p>Refresh Token: <%= refresh_token %>
        </p>

        <form action="http://localhost:3009/api/v1/profile" method="get">
            <button>Get Profile</button>
        </form>

        <form action="http://localhost:3008/api/v1/auth/logout/google" method="post">
            <button>Logout</button>
        </form>
        <%} else{ %>
            <form action="http://localhost:3008/api/v1/auth/login/federated/google" method="get">
                <button>Login with google</button>
            </form>
            <% } %>
</body>

</html>